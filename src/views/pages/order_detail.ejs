<!DOCTYPE html>
<html>
<head>
    <title>Chi tiết đơn hàng</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body style="background:#f6f8fa;font-family:sans-serif;margin:0;">
    <div style="max-width:900px;margin:32px auto;">
        <h2 style="color:#222;margin-bottom:24px;">Chi tiết đơn hàng <%= order.UID %></h2>
        <ul style="list-style:none;padding:0;margin-bottom:24px;">
            <li style="margin-bottom:8px;">
                Trạng thái: 
                <% if(order.Status === 1) { %>
                    <span style="background:#ffc107;color:#222;padding:3px 12px;border-radius:12px;font-size:0.95em;">Chờ thanh toán</span>
                <% } %>
                <% if(order.Status === 2) { %>
                    <span style="background:#28a745;color:#fff;padding:3px 12px;border-radius:12px;font-size:0.95em;">Đã thanh toán</span>
                <% } %>
                <% if(order.Status === 3) { %>
                    <span style="background:#17a2b8;color:#fff;padding:3px 12px;border-radius:12px;font-size:0.95em;">Đã giao</span>
                <% } %>
                <% if(order.Status === 4) { %>
                    <span style="background:#dc3545;color:#fff;padding:3px 12px;border-radius:12px;font-size:0.95em;">Đã hủy</span>
                <% } %>
            </li>
            <li style="margin-bottom:8px;">Tổng tiền: <strong><%= order.Total_Price %></strong></li>
            <li style="margin-bottom:8px;">Ngày tạo: <%= order.createdAt.toLocaleString() %></li>
        </ul>
        <h3 style="margin-bottom:16px;">Danh sách món</h3>
        <table style="width:100%;border-collapse:collapse;background:#fff;border-radius:10px;overflow:hidden;box-shadow:0 2px 15px rgba(0,0,0,0.07);">
            <thead>
                <tr style="background:#f8f9fa;">
                    <th style="padding:12px 8px;text-align:left;">Tên món</th>
                    <th style="padding:12px 8px;text-align:left;">Số lượng</th>
                    <th style="padding:12px 8px;text-align:left;">Giá</th>
                </tr>
            </thead>
            <tbody>
                <% order.items.forEach(item => { %>
                    <tr style="border-bottom:1px solid #eee;">
                        <td style="padding:10px 8px;"><%= item.FoodId.Name %></td>
                        <td style="padding:10px 8px;"><%= item.Quantity %></td>
                        <td style="padding:10px 8px;"><%= item.FoodId.Price %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
        <a href="/view/orders" 
           style="display:inline-block;margin-top:18px;padding:6px 16px;background:#6c757d;color:#fff;border:none;border-radius:4px;text-decoration:none;font-weight:500;">
            <i class="fa fa-arrow-left"></i> Quay lại
        </a>
    </div>
</body>
</html>